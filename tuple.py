# Program to find minimum and maximum values in tuples
# Tuples are immutable sequences of elements

# Define two sample tuples with integer values
tuple1 = (2, 6, 46, 74, 3, 6, 3, 6)
tuple2 = (4, 68, 9, 7, 5, 78, 5, 8)

# Find minimum and maximum values in tuple1
min_value = min(tuple1)
max_value = max(tuple1)

# Display the results
print("minimum : ", min_value)
print("maximun : ", max_value)

