# Program to generate a random number
# Uses the random module to generate a random integer

import random

# Generate and display a random integer between 1 and 1000 (inclusive)
print(f"Random number between 1 and 1000: {random.randint(1, 1000)}")